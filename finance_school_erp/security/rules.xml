<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <data>
        <!--For restriction of Students-->
        <record id="rule_finance_student" model="ir.rule">
            <field name="name">Student Finance Rule: Students se they own records</field>
            <field name="model_id" ref="model_finance_finance"/>
            <field name="domain_force">[('student_id.user_id', '=', user.id)]</field>
            <field name="groups" eval="[(4, ref('base_school_erp.group_school_student'))]"/>
        </record>
    </data>
</odoo>