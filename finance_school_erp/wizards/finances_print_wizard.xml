<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <data>
        <record model="ir.ui.view" id="demo_failures_dialog">
            <field name="name">Print Finances</field>
            <field name="model">print.finances.wizard</field>
            <field name="arch" type="xml">
                <form>
                    <group>
                        <field name="student_id" options="{'no_open':True}"/>
                        <field name="start_date" string="From:"/>
                        <field name="last_date" string="To: "/>
                    </group>
                    <footer>
                        <button name="student_finances" string="Print" type="object"/>
                        <button string="Cancel" class="btn-secondary" special="cancel"/>
                    </footer>
                </form>
            </field>
        </record>

        <record id="action_print_finances" model="ir.actions.act_window">
            <field name="name">Print Finances</field>
            <field name="res_model">print.finances.wizard</field>
            <field name="view_mode">form</field>
            <field name="context">{'default_student_id': active_id}</field>
            <field name="target">new</field>
        </record>
    </data>

</odoo>